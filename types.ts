export type Jobs = {
    id: number,
    title: string
    company: string
    location: string
    type: string
    salary: string
    description: string,
    requirements: string[],
    benefits: string[]
}
